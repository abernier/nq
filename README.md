# Install

```
npm install nQuery
```

# Usage

## Client side

```html
<script src="nQuery/lib/index.js"></script>
```

## Server-side

```coffeescript
$ = require 'nQuery'
```

```coffeescript
$1 = require('nQuery').create(window1)
$2 = require('nQuery').create(window2)
```